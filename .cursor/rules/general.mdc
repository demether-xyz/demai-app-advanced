---
description: 
globs: 
alwaysApply: true
---
# General

- We are building the UI/UX for the brand, generalize as you go from styles/global.css to reusable components
- Front and Back end are running and reload automatically
- Use clean and non-verbose code
- For Backend, always use poetry, we use FastAPI
- For Frontend use pnpm